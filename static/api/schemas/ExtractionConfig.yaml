title: ExtractionConfig
type: object
properties:
  taxonomy_id:
    title: taxonomy
    type: string
    example: taxonomy
  parent_taxonomy_id:
    title: parent_taxonomy_id
    type: string
    example: parent_taxonomy_id
  version:
    title: version
    type: string
    example: version
  graph_id:
    title: graph
    type: string
    example: graph_id
  active:
    title: active
    type: boolean
    example: true
  type:
    title: type
    type: string
    enum:
    - digestion
    - enrichment
    - trending
    example: digestion
required:
- taxonomy_id
- graph_id