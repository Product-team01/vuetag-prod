properties:
  dag_id:
    description: This field describes the unique reference to the deployed DAG associated
      with the audience.
    title: Dag Id
    type: string
  message:
    description: This field shows a short message about the output of the deploy status
      API.
    title: Message
    type: string
required:
- message
- dag_id
title: AudienceDeployEntity
type: object
