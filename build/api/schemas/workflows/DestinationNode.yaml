properties:
  dependent_node_ids:
    default: []
    description: This field describes the list of node IDs that are dependent on the
      current node. This is used to build the workflow DAG.
    items:
      type: string
    title: Dependent Node Ids
    type: array
  destination_id:
    description: This field describes the identifier of connector hub's destination
    title: Destination Id
    type: string
  node_id:
    default: ''
    description: This field describes the identifier of the corresponding destination
      node. If it is not present, it will be created.
    title: Node Id
    type: string
required:
- destination_id
title: DestinationNode
type: object
