properties:
  condition_operations:
    description: This field describes the feature configs inside the rule.
    items:
      $ref: ../workflows/FeatureConfig.yaml
    title: Condition Operations
    type: array
  condition_operator:
    description: This field describes the operator with which the features inside
      the condition have to be combined.
    title: Condition Operator
    type: string
required:
- condition_operator
- condition_operations
title: Condition
type: object
